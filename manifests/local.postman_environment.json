{
  "name": "AI Pipeline — Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://127.0.0.1:3000",
      "type": "default",
      "enabled": true
    },
    { "key": "workflow", "value": "content-enrichment", "type": "default", "enabled": true },
    { "key": "service", "value": "summary", "type": "default", "enabled": true },
    { "key": "triggerId", "value": "content-published", "type": "default", "enabled": true }
  ],
  "_postman_variable_scope": "environment",
  "_comment": "Only configuration lives here. `runId` and `deploymentId` are deliberately absent: the collection's own tests write them at run time, and an environment value — even an empty one — takes precedence over a collection variable, which would blank them out again."
}
